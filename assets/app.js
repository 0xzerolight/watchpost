// Client behaviour for watchpost: chart rendering, marker overlays, kind
// filters, sparklines and the theme toggle.
//
// Stub: every entry point is implemented in Task 14. The names exist now so
// pages rendered before then can call them without throwing.
window.watchpost = {
  initRepoCharts() {},
  refreshMarkers() {},
  toggleKind() {},
  initSparklines() {},
  applyTheme() {},
};
