# Copy to .env. Every line is optional — watchpost starts without any of them
# and asks for a GitHub token in the browser.

# Optional overrides — defaults shown.

# Fine-grained personal access token with, under Repository permissions:
#   Metadata: read          — repository list and basic counts
#   Administration: read    — traffic views/clones/referrers/popular paths
#   Contents: read          — releases and asset download counts
#   Pull requests: read     — open pull request count
# A classic token with `repo` also works. A missing permission 403s that one
# call and nothing else: without Administration: read the traffic charts stay
# empty while the rest of the data still lands.
#
# Leave this unset to paste the token into the setup page instead, which saves
# it to the database. Setting it here wins over a saved token and removes the
# form from the settings page.
#WATCHPOST_GITHUB_TOKEN=
#WATCHPOST_CRON=0 5 * * * *          # six fields (seconds first), UTC
#WATCHPOST_TZ=UTC                    # IANA name, e.g. Europe/Madrid; display only
#WATCHPOST_HOST=127.0.0.1            # the image sets 0.0.0.0
#WATCHPOST_PORT=8080                 # the port the binary binds; inside the image it stays 8080
#WATCHPOST_LOG=info                  # tracing EnvFilter, e.g. watchpost=debug
#WATCHPOST_DB_PATH=./data/watchpost.db
#WATCHPOST_GITHUB_API_BASE=https://api.github.com   # GitHub Enterprise: …/api/v3/

# Container plumbing, read by compose and the entrypoint rather than by the
# binary. The installer writes these.
#WATCHPOST_HOST_PORT=8080            # the port published on the host
#PUID=1000                           # host uid/gid the container runs as, so
#PGID=1000                           # the bind-mounted ./data stays writable
